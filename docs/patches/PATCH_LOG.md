# Patch Log

This is a human-readable index of patch zips stored in `docs/patches/`.

Add entries via:

```bash
python3 docs/tools/register_patch.py --zip /path/to/patch.zip --title "..." --summary "..."
```

---

